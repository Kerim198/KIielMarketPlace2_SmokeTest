package pages;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class US018_HomePage extends BasePage {
    @FindBy (id = "dropdownMenuButton1")
    public WebElement profilimButton;
}
